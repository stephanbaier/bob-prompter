# Anthropic API key for LLM Reality-Check (claude-haiku-4-5)
# Optional — wenn nicht gesetzt, läuft Tool nur mit Pure-JS-Matcher.
# Lokal: in .env.local kopieren (gitignored).
# Vercel: als Project-Env-Var setzen.
ANTHROPIC_API_KEY=sk-ant-...
